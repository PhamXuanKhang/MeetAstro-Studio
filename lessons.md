[2026-05-13] | Backend `/recording/stop` crash vì dùng `self._ws.open` với `websockets` object `ClientConnection` không có attribute này | Bỏ check `.open`, gửi/đóng khi `_ws` tồn tại và để WebSocket API raise nếu connection đã đóng | Không dựa vào thuộc tính trạng thái không ổn định giữa các version dependency; ưu tiên gọi API idempotent (`close`) hoặc bắt exception ở boundary.
[2026-05-13] | UI báo nhầm lỗi quyền microphone khi Python recorder IPC timeout trong lúc start chậm | Tăng timeout IPC lên 30s và hiển thị message lỗi thật từ exception | Không dùng catch-all message chẩn đoán hẹp cho nhiều nguyên nhân; surface lỗi thật ở UI khi boundary async fail.
[2026-05-13] | Python recorder IPC timeout giả vì React StrictMode có thể chạy auto-start effect 2 lần trong dev, trong khi Electron `PythonRecorder` chỉ lưu một `pendingResolve` và request sau ghi đè request trước | Chặn auto-start trùng bằng `useRef`, reject concurrent IPC calls và thêm log action/response/timeout | Không để bridge IPC single-flight bị ghi đè silent; với React dev StrictMode phải guard side effect khởi động thiết bị/process.
[2026-05-13] | Live transcript không hiện dù chunk upload 200 OK vì SSE chỉ emit khi `len(lines)` tăng và lọc `speaker == -2`; stop còn timeout vì sidecar flush toàn bộ chunk backlog trước EOF | Emit SSE theo snapshot JSON, map unknown speaker thành `Speaker ?`, thêm log WS/SSE, giới hạn drain backlog khi stop và vẫn trả response nếu stream stop lỗi | Với realtime transcript, không dùng số dòng làm change detector và không block UI stop trên backlog network.
[2026-05-13] | Debug live audio khó vì log cũ chỉ có HTTP 200 và không cho biết audio có tiếng, bytes đi qua từng tầng, WhisperLiveKit trả schema gì, hay parser bỏ dòng vì sao | Thêm log sidecar produced/posted/RMS/peak/drops, backend chunk/WS raw/parser stats và frontend SSE raw/segments | Với pipeline realtime nhiều hop, log theo từng boundary và in sample raw payload trước khi đoán lỗi provider/audio.
[2026-05-13] | Live audio stream drop gần hết vì gửi từng frame 320B/10ms qua HTTP multipart, tạo ~100 requests/s và làm queue full | Gom frame thành packet 16KB (~0.5s audio) trước khi POST, flush phần còn lại khi stop | Khi bridge audio realtime qua HTTP, packet hóa theo 0.5–1s thay vì gửi từng callback frame nhỏ.
[2026-05-14] | WhisperLiveKit có thể trả live text trong `buffer_transcription` với `lines=[]`, khiến UI không hiện transcript nếu chỉ parse committed diarized lines | Lưu `buffer_transcription` trong stream session và map thành segment fallback `Speaker ?` khi `lines` rỗng; tăng packet lên 32KB (~1s) để giảm backlog | Với WLK realtime, hiển thị buffer transcription cho partial UI và coi `lines` là committed/final hơn, không phải nguồn text duy nhất.
[2026-05-14] | WhisperLiveKit partial output có thể chứa timestamp control tokens như `<|27.14|>` làm transcript UI nhiễu và transcript container không tự cuộn | Sanitize timestamp tokens trước khi set state và auto-scroll transcript container khi segments thay đổi | Với live transcript UI, lọc provider control tokens ở boundary render và giữ viewport bám dòng mới nhất.
