# Website reads Windows EXE download metadata from backend `/downloads/metadata.json`.
# Backend resolves release info via APP_DOWNLOAD_GITHUB_REPO / APP_DOWNLOAD_URL.

# Static media served from `website/public` by Vite.
VITE_HERO_IMAGE_URL=/hero-preview.png

# Optional YouTube embed URL. Leave empty to show the built-in product walkthrough placeholder.
VITE_DEMO_EMBED_URL=
